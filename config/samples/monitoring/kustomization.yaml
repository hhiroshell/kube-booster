apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - monitoring.yaml

configMapGenerator:
  - name: grafana-dashboard-kube-booster
    namespace: monitoring
    files:
      - kube-booster.json=grafana-dashboard.json
    options:
      disableNameSuffixHash: true
